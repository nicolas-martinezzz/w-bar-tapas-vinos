/** Default capacity when seeding or creating tables without explicit value */
export const DEFAULT_MESA_CAPACITY = 8;

/** Default booking slot length in minutes */
export const DEFAULT_RESERVATION_SLOT_MINUTES = 90;
